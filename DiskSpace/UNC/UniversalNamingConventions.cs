using System.Runtime.InteropServices;
using DiskSpace.Api;
using DiskSpace.UNC.Windows;

namespace DiskSpace.UNC
{
    public class UniversalNamingConventions
    {
        /*
        private static bool IsUnix()
        {
            var isUnix = RuntimeInformation.IsOSPlatform(OSPlatform.OSX) ||
                         RuntimeInformation.IsOSPlatform(OSPlatform.Linux);

            return isUnix;
        }

        public static DiskSpaceService.DriveData UncDriveInfo()
        {
            switch (IsUnix())
            {
                case true:
                    break;
                case false:
WindowsDiskSpace.FileSystemProperties.GetProperties()
            }
        }
        */
    }
}
